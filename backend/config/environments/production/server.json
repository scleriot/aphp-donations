{
  "host": "${process.env.STRAPI_HOST || '127.0.0.1'}",
  "port": "${process.env.STRAPI_PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
